# Keep the published site limited to the app itself.
marketing/
.claude/
.obsidian/

# Local-only / build artifacts (Pages serves the multi-file index.html directly)
uplvl.html
style.prev.css
landing.prev.html

# Private notes / data — synced to Google Drive, never published to the public repo
PROJECT-INFO.md
uplvl-backup.json

# OS / editor cruft
.DS_Store
Icon?
*.tmp
*~

# Local Netlify folder
.netlify
